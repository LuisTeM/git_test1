Es un programa para crear una lista apartir de las variables que ingrese el usuario. el usuario puede agregar tantos elementos como desee
hasta que el usuario ingrese la respuesta no.
Sin embargo, hay un pequeño detalle. el programa le preguntara al usuario si iniciara una nueva lista, el usuario tiene que ingresar
una respuesta valida, mientras que si va a agregar un nuevo elemento a la lista el programa se detendra hasta que el usuario ingrese
especificamente la palabra "no".

